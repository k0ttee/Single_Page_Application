#######################
# 192.168.1.2         #
# Finance server      #
# Provide Finance API #
#######################

server {
        server_name 192.168.1.2;
        root /mnt/c/Users/Азъ/wsl-localhost/192.168.1.2;

        listen 443 ssl http2;
        #listen [::]:443 ssl http2 ipv6only=on;

        index index.php;

        location / {
                add_header 'Access-Control-Allow-Origin' 'http://localhost';
                expires 0;
                etag off;
                include snippets/fastcgi-php.conf;
                fastcgi_pass unix:/run/php/php7.4-fpm.sock;
        }

        ssl_certificate     /etc/letsencrypt/live/192.168.1.2/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/192.168.1.2/privkey.pem;
}
